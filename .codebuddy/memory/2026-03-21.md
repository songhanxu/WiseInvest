## 2026-03-21

### 环境配置
- 配置了 DeepSeek API Key 到 `backend/.env`（model: deepseek-chat, base_url: deepseek.com/v1）
- 修复了 `start.sh` 中的 3 个问题：API Key 检查逻辑、数据库密码不一致、日志目录创建顺序
- 更新了 iOS `APIConfig.swift`：IP 改为 192.168.1.4，清除了过期的 ngrok 隧道地址

### 腾讯云部署方案
- 创建了 `backend/Dockerfile`（多阶段构建，alpine 基础镜像）
- 创建了 `docker-compose.yml`（Go 后端 + PostgreSQL 15 + Redis 7 + Nginx + Certbot）
- 创建了 `backend/.env.production`（生产环境配置模板）
- 创建了 `nginx/nginx.conf` + `nginx/conf.d/wiseinvest.conf`（反向代理 + SSL + SSE 支持）
- 创建了 `deploy.sh`（腾讯云一键部署脚本）
- 创建了 `backend/.dockerignore`
- 推荐方案：腾讯云轻量应用服务器 2核4G，约 50-80 元/月
